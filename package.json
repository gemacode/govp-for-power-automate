{
  "name": "govp-for-power-automate",
  "version": "0.2.0",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-connector.mjs",
    "package": "node scripts/package.mjs",
    "check": "npm run validate && npm test && npm run package"
  }
}
